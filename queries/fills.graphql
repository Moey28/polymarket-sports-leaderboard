# NOTE: timestamps are BigInt in many subgraphs
query ($first: Int!, $skip: Int!, $fromTs: BigInt!, $toTs: BigInt!) {
  filledEvents(
    first: $first
    skip: $skip
    orderBy: timestamp
    orderDirection: asc
    where: { timestamp_gte: $fromTs, timestamp_lte: $toTs }
  ) {
    id
    timestamp
    txHash
    amount
    price
    outcome
    maker
    taker
    market
  }
}
